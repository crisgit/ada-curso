// Variáveis

let idade = 26;

console.log(idade);

// Tipos de variáveis

idade = 30;
let numero = 30;
let altura = 1.77;
let nome = 'Cristina';
let estudando = true;

console.log(numero, typeof numero);
console.log(altura, typeof altura);
console.log(nome, typeof nome);
console.log(estudando, typeof estudando);

let outra; // tipo não declarado

console.log(outra);

let nula = null;

console.log(nula);

var variavel = 30; // utilizado muito em código legado, mas depois do es15 não é recomendado utilizar o var e usar somente o let

const constante = 100; // tipo de variavel constante que não permite mudança de valores na variavel

